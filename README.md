# Companion page of submission #78

- The `gitlogs` folder contains the logs extracted from the use-case versioning history

- the `manual-analysis` contains the _service dependencies_, _feature mapping_ and _codevelopment top rank_ files, manually extracted from the use-cases repository or from our codevelopment analysis.
- the `src` folder contains the `java` code to analyse the `gitlogs` and output `csv` in the `csv` folder. (Main: `Main.java`)
- the `csv` folder contains the codevelopement data
- the `R` folder consumes those `csv` and  contains the R script needed to update the graphs. (Main: `R.Rproj`)

